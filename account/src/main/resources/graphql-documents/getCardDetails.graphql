query getCardDetails($mobileNumber: String!) {
    card(mobileNumber: $mobileNumber) {
        mobileNumber
        cardNumber
        cardType
        totalLimit
        amountUsed
        availableAmount
    }
}