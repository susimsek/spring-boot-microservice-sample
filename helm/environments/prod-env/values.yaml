config-server:
  enabled: true
  spring:
    profiles:
      active: prod

eureka-server:
  enabled: true
  spring:
    profiles:
      active: prod

gateway-server:
  enabled: true
  spring:
    profiles:
      active: prod

account:
  spring:
    profiles:
      active: prod
    liquibase:
      contexts: prod

loan:
  spring:
    profiles:
      active: prod
    liquibase:
      contexts: prod

card:
  spring:
    profiles:
      active: prod
    liquibase:
      contexts: prod

message:
  spring:
    profiles:
      active: prod